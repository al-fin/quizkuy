import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  container: {
    width: '600px',
  },
  title: {
    fontWeight: 'bold',
  },
  end: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
}));
